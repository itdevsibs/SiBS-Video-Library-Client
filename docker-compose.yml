services:
  web:
    container_name: sibs-video-library-client
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL}
        VITE_APP_NAME: ${VITE_APP_NAME}
    ports:
      - "5008:80"
    restart: unless-stopped